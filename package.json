{
  "name": "datedrop-api",
  "version": "1.0.0",
  "description": "DateDrop - Orthodox Jewish Dating App API",
  "main": "backend/src/server.js",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && npm run dev",
    "migrate": "cd backend && npm run migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["dating", "matching", "orthodox", "jewish"],
  "author": "DateDrop",
  "license": "Proprietary",
  "engines": {
    "node": "18.x"
  }
}
