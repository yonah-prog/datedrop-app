{
  "name": "datedrop-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "DateDrop - Orthodox Jewish Dating App",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "echo 'Use cd backend && npm run dev OR cd frontend && npm run dev'",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm install && node src/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
